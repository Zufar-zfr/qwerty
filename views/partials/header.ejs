<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кабинет врача-терапевта</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="logo">Кабинет врача</a>
            <div class="nav-links">
                <% if (typeof doctorName !== 'undefined') { %>
                    <span>Врач: <%= doctorName %></span>
                    <a href="#" id="logoutBtn">Выйти</a>
                <% } else { %>
                    <a href="/doctor/login">Вход</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main>